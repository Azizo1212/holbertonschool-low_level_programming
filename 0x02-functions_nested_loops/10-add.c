#include <stdio.h>
#include "main.h"

/**
 * add:adds two integers and returns the result.
 * @a: adds two integers
 * @b: adds tow integers
 * Return: a+b
 */
int add(int a, int b)
{
	return (a + b);
}
